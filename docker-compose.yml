services:
  anchor-lfs:
    build: .
    ports:
      - "5420:5420"
    volumes:
      - ./data:/app/data
      - ./config.toml:/app/config.toml:ro
    environment:
      - ANCHOR_LFS_LISTEN=:5420
    restart: unless-stopped
